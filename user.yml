- hosts: all
  become: yes
  vars:
    password: student
  tasks:
    - name: create the admin user
      user: name=admin append=yes state=present createhome=yes shell=/bin/bash password="{{password | password_hash('sha512')}}"
    - name: give admin passwordless sudo
      lineinfile:
        dest: /etc/sudoers
        line: 'admin ALL=(ALL) NOPASSWD: ALL'
        validate: 'visudo -cf %s'

    - name: Update & upgrade
      apt: 
        update_cache: yes
        cache_valid_time: 3600
        upgrade: dist  

    - name: Check of reboot nodig is
      stat:
        path: /var//run/reboot-required
      register: reboot_required_file

    - name: Reboot als nodig
      renoot:
      when: reboot_required_file.stat.exusts == true

    - name: Install ACL
      apt: 
        name: acl
        state: latest